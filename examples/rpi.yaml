version: '3.3'
services:
  workstation:
    container_name: peach
    hostname: peach
    networking: host
    environment:
      - TZ=America/Central 
    volumes:
      - /storage/workstation:/workstation
